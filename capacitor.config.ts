import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.salim.retrobrickbreaker',
  appName: 'DXball classic',
  webDir: 'dist'
};

export default config;
